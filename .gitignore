# Build artifacts
/dist/
/columbus
*.test

# Zig cross-compile cache (may exist from older local builds)
.zig-cache/

# Local data / coverage
*.sqlite
*.sqlite-wal
*.sqlite-shm
coverage.out
coverage.html

# Columbus local config (also git-excluded by `init`, belt-and-suspenders here)
.columbus.json

# Embedding model weights: fetched via `make fetch-model`, never committed raw.
/internal/embed/assets/model.onnx
/internal/embed/assets/model.safetensors

# OS noise
.DS_Store
.claude
