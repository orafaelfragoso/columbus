# Build artifacts
/dist/
/columbus
*.test

# Zig cross-compile cache (created by zig cc during cgo release builds)
.zig-cache/

# Local data / coverage
*.sqlite
*.sqlite-wal
*.sqlite-shm
coverage.out
coverage.html

# Columbus local config (also git-excluded by `init`, belt-and-suspenders here)
.columbus.json

# Embedding model weights: fetched via `make fetch-model`, never committed raw
# (too large). tokenizer.json next to it is small and committed.
/internal/embed/assets/model.onnx

# OS noise
.DS_Store
.claude
